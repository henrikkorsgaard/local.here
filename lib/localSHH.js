/*global console, process, require, __filename, module*/
( function () {
    'use strict';
    module.exports = api();

    function localSSH() {

        function init(wsConnection){

        }

        function terminate(wsConnection){

        }

        return Object.freeze( {
            init,
            terminate
        } );
    }
}() );
